update-database-replication:
    description: "Update database entries for replication tests."
    module_name: "tests.update_database_replication"
    class_name: "UpdateDatabaseReplication"
    enabled: true
    args:
    kwargs:
      database:
        type: es
        uri: http://130.246.214.144:80/monit/metadata/
        index: "[replication]"
        search_range_lte: now
        search_range_gte: now-7d
  