test-stub:
  description: "Test stub"
  module_name: "tests.stub"
  class_name: "TestStub"
  enabled: false
  args:
  kwargs:
    text: "Hello World!"
test-replication:
  description: "Test uploading/replication to RSEs."
  module_name: "tests.replication"
  class_name: "TestReplication"
  enabled: true
  args:
  kwargs:
    n_files: 1
    sizes:
      - 100000 # bytes
    lifetime: 2592000 # seconds                
    rses:
      - ALPAMED-DPM
      - CNAF-STORM
      - DESY-DCACHE
      - EULAKE-1
      - EULAKE-2
      - GSI-ROOT
      - IN2P3-CC-DCACHE
      - INFN-NA-DPM
      - LAPP-DCACHE
      - LAPP-DPM
      - PIC-DCACHE
      - PIC-INJECT
      - SARA-DCACHE
    scope: rucio-testing
test-replicate-dir:
  description: "Test uploading/replication of directory to RSEs."
  module_name: "tests.replicate_dir"
  class_name: "TestReplicateDir"
  enabled: false
  args:
  kwargs:
    n_files: 3
    file_size: 13000 # bytes
    lifetime: 3600 # seconds                
    rses:
      # - ALPAMED-DPM
      # - CNAF-STORM
      - DESY-DCACHE
      # - EULAKE-1
      # - EULAKE-2
      # - GSI-ROOT
      # - IN2P3-CC-DCACHE
      # - INFN-NA-DPM
      # - LAPP-DCACHE
      # - LAPP-DPM
      # - PIC-DCACHE
      # - PIC-INJECT
      - SARA-DCACHE
    scope: rucio-testing