sync-database-replication:
    description: "Sync database with replication tests done on the datalake"
    module_name: "tasks.replication_sync_db"
    class_name: "SyncDatabaseReplication"
    enabled: true
    args:
    kwargs:
      database:
        type: es
        uri: http://130.246.214.144:80/monit/metadata/
        index: "[replication]"
        search_range_lte: now
        search_range_gte: now-24h
        max_rows: 10000
      
  